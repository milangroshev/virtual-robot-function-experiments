# =============================================================================
# HOST 1 (10.205.10.16) — Robot Simulation Only (Two Robots)
# =============================================================================
# Runs ONLY: gazebo + robot spawners for panther and panther2.
# Nav2 stacks run on HOST 2 (10.205.10.5) and controllers run on HOST 3.
# =============================================================================

x-common-config:
  &common-config
  env_file: ../.env
  environment:
    - GZ_IP=127.0.0.1
    - IGN_IP=127.0.0.1

x-nav2-common:
  &nav2-common
  <<: *common-config
  image: nav2-profiling:latest
  build:
    context: ../..
    dockerfile: docker/Dockerfile
  environment:
    - ROS_DOMAIN_ID=${ROS_DOMAIN_ID:-0}
    - RMW_IMPLEMENTATION=${RMW_IMPLEMENTATION:-rmw_cyclonedds_cpp}
    - CYCLONEDDS_URI=file:///config/cyclonedds-robot.xml
  volumes:
    - ../config:/config
    - ./config/cyclonedds-robot.xml:/config/cyclonedds-robot.xml
    - ../../maps:/maps
    - ../../husarion_ugv_navigation:/ros2_ws/src/husarion_ugv_autonomy_ros/husarion_ugv_navigation
  network_mode: "host"
  ipc: host

volumes:
  shared_tmp:

services:
  # ===========================================================================
  # 0. GAZEBO — World + Robot 1 (panther)
  # ===========================================================================
  gazebo:
    image: husarion/husarion-ugv-gazebo:jazzy-add-parameter-to-disable-orientation
    container_name: gazebo
    <<: *common-config
    runtime: nvidia
    environment:
      - DISPLAY
      - CYCLONEDDS_URI=file:///config/cyclonedds-robot.xml
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=all
    devices:
      - /dev/dri:/dev/dri
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
      - ../config:/config
      - ./config/cyclonedds-robot.xml:/config/cyclonedds-robot.xml
      - shared_tmp:/tmp
      - ../gazebo_overrides/worlds/husarion_world.sdf:/ros2_ws/src/husarion_gz_worlds/worlds/husarion_world.sdf:ro
      - ../gazebo_overrides/worlds/husarion_world.sdf:/ros2_ws/install/husarion_gz_worlds/share/husarion_gz_worlds/worlds/husarion_world.sdf:ro
      - ../gazebo_overrides/urdf/velodyne_puck.urdf.xacro:/ros2_ws/src/husarion_components_description/urdf/velodyne_puck.urdf.xacro:ro
      - ../gazebo_overrides/urdf/velodyne_puck.urdf.xacro:/ros2_ws/install/husarion_components_description/share/husarion_components_description/urdf/velodyne_puck.urdf.xacro:ro
    network_mode: "host"
    ipc: host
    healthcheck:
      test: >
        bash -c "/ros_entrypoint.sh ros2 topic echo --timeout 0.2
        --field header.frame_id /${ROBOT_NAMESPACE:-panther}/odometry/filtered
        | grep -q odom"
      interval: 4s
      timeout: 3s
      retries: 4
    command: >
      ros2 launch husarion_ugv_gazebo simulation.launch.py
        components_config_path:=/config/components.yaml
        namespace:=${ROBOT_NAMESPACE:-panther}
        robot_model:=${ROBOT_MODEL:-panther}
        gz_headless_mode:=True
        use_rviz:=False

  # ===========================================================================
  # 0b. ROBOT 2 SPAWNER
  # ===========================================================================
  gz_robot_2:
    image: husarion/husarion-ugv-gazebo:jazzy-add-parameter-to-disable-orientation
    container_name: gz_robot_2
    <<: *common-config
    runtime: nvidia
    environment:
      - DISPLAY
      - CYCLONEDDS_URI=file:///config/cyclonedds-robot.xml
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=all
    devices:
      - /dev/dri:/dev/dri
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
      - ../config:/config
      - ./config/cyclonedds-robot.xml:/config/cyclonedds-robot.xml
      - shared_tmp:/tmp
      - ../gazebo_overrides/urdf/velodyne_puck.urdf.xacro:/ros2_ws/src/husarion_components_description/urdf/velodyne_puck.urdf.xacro:ro
      - ../gazebo_overrides/urdf/velodyne_puck.urdf.xacro:/ros2_ws/install/husarion_components_description/share/husarion_components_description/urdf/velodyne_puck.urdf.xacro:ro
    network_mode: "host"
    ipc: host
    depends_on:
      gazebo:
        condition: service_healthy
        restart: true
    healthcheck:
      test: >
        bash -c "/ros_entrypoint.sh ros2 topic echo --timeout 0.2
        --field header.frame_id /panther2/odometry/filtered
        | grep -q odom"
      interval: 4s
      timeout: 3s
      retries: 6
      start_period: 20s
    command: >
      ros2 launch husarion_ugv_gazebo simulate_robot.launch.py
        namespace:=panther2
        robot_model:=${ROBOT_MODEL:-panther}
        components_config_path:=/config/components.yaml
        add_world_transform:=True
        x:=11.5
        y:=-9.3
        z:=0.2
        yaw:=1.39

  # ===========================================================================
  # ROBOT 1 sensor_preproc (namespace = panther)
  # ===========================================================================

  sensor_preproc:
    <<: *nav2-common
    container_name: ${ROBOT_NAMESPACE:-panther}_sensor_preproc
    depends_on:
      gazebo:
        condition: service_healthy
    healthcheck:
      test: >
        bash -c "/ros_entrypoint.sh ros2 topic echo --timeout 0.5
        --field header.frame_id /${ROBOT_NAMESPACE:-panther}/velodyne_points_filtered
        | grep -q velodyne"
      interval: 4s
      timeout: 3s
      retries: 6
      start_period: 20s
    command: >
      ros2 launch husarion_ugv_navigation sensor_preproc_launch.py
        namespace:=${ROBOT_NAMESPACE:-panther}
        observation_topic:=/${ROBOT_NAMESPACE:-panther}/velodyne_points
        observation_topic_type:=pointcloud
        use_sim_time:=True
        robot_model:=${ROBOT_MODEL:-panther}

  # ===========================================================================
  # ROBOT 2 sensor_preproc (namespace = panther2)
  # ===========================================================================

  sensor_preproc_2:
    <<: *nav2-common
    container_name: panther2_sensor_preproc
    depends_on:
      gz_robot_2:
        condition: service_healthy
    healthcheck:
      test: >
        bash -c "/ros_entrypoint.sh ros2 topic echo --timeout 0.5
        --field header.frame_id /panther2/velodyne_points_filtered
        | grep -q velodyne"
      interval: 4s
      timeout: 3s
      retries: 6
      start_period: 20s
    command: >
      ros2 launch husarion_ugv_navigation sensor_preproc_launch.py
        namespace:=panther2
        observation_topic:=/panther2/velodyne_points
        observation_topic_type:=pointcloud
        use_sim_time:=True
        robot_model:=${ROBOT_MODEL:-panther}
